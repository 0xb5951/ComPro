#include<iostream>
#include <string>
#include <cstdio>
using namespace std;

int main(){
  int N;
  cin >> N;
  int A[N] = 0;
  // for(i = 0; i < N; i++){
    scanf("%d", A);
  // }
  int count = 0;

  for(int i = 0; i < s.length(); i++){
    if(s[i] == '1')
      count++;
  }

  cout << count << endl;
}
